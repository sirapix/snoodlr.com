# snoodlr.com

Astro + Tailwind static marketing site for Snoodlr.

## Commands

```sh
npm install
npm run dev
npm run build
```

The public site includes home, pricing, and contact pages, plus static `sitemap.xml`, `robots.txt`, canonical URLs, Open Graph metadata, and JSON-LD structured data.
